# Test dependencies for HVAC Control Center.
# Install: pip install -r requirements_test.txt
# Run: pytest tests/ -v
# home-assistant-frontend provides hass_frontend so the "frontend" dependency can load in CI.

pytest>=7.0.0
pytest-asyncio>=0.21.0
homeassistant>=2024.1.0
home-assistant-frontend
pytest-homeassistant-custom-component>=0.13.0
pre-commit>=3.0.0
ruff>=0.8.0
